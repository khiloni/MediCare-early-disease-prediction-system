name: health_assistant
description: "Medicare AI — AI Health Assistant with Doctor Verification"
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  supabase_flutter: ^2.3.0
  provider: ^6.1.1
  url_launcher: ^6.2.2
  intl: ^0.19.0
  google_fonts: ^6.1.0
  percent_indicator: ^4.2.3
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  flutter_svg: ^2.0.9
  google_sign_in: ^6.1.6
  go_router: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
